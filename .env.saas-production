# Production mobile build — copy to .env.saas.local (gitignored), then:
#   npm run build:mobile:saas:prod
#
# Vite loads .env.saas.local when building with --mode saas.
# Use your PROD Vercel URL (HTTPS). Do not use the demo site URL.

VITE_APP_MODE=saas
VITE_SAAS_API_URL=https://swift-pos-pied.vercel.app
